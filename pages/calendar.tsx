import Calendar from '@/components/Organisms/Calendar';
import * as React from 'react';

export interface IAppProps {
}

export default function CalendarPage(props: IAppProps) {
    return (
        <Calendar />
    );
}
